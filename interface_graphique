import tkinter as tk

#fenetre

root = tk.Tk()
label = tk.Label(root, text="", font=("courrier", "40"))
label.grid(row=0, column=0)

#widget button

bouton = tk.Button(root, text="création de la configuration aléatoire", font=("courrier", "30"))
bouton.grid()


root.mainloop()

